#!/usr/bin/env python
import simplejson
import urllib2

URL = "https://api.tradehill.com/APIv1/USD/Orderbook"
request = urllib2.Request(URL)

content = urllib2.urlopen(request).read()
ticker = simplejson.loads(content)['asks']

ticker = [[str(i) for i in j] for j in ticker]
print ticker
