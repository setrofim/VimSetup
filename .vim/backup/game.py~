#!/usr/bin/python
import os
import pickle
import time
from random import choice

timestamp = time.asctime(time.localtime(time.time()))

class GameCharacter(object):
    def __init__(self):
          self.strength = 0
          self.endurance = 0
          self.intelligence = 0
          self.inventory = []

class Monster(GameCharacter):
    pass

class GenerateCharacter(GameCharacter): # Changed to inherit from GameCharacter
    '''Generate a random character for the game'''

    def __init__(self, age, name = 'troll'):
          #GameCharacter.__init__(self)  # commented this part out because it causes an error. Not sure what it does either.
          self.powers = ['fire','water','healing','fly']
          self.name = name; self.age = int(age); self.power = choice(self.powers)
   
    def returner(self):
          if self.age < 2:
                    __years = 'year old'
          else:
                    __years = 'years old'
          return "Your name is {0}, you are {1} {2}and your power is {3}".format(self.name,self.age,__years,self.power)
   
          def use_power(self, target):
                    return apply_power(self.power, target)

def print_o_number():
          for number in range(65, 122):
                    if number % 2:
                              print " " * ((number % 5) / 2), "*" * ((number % 13) / 3),

def f(x):
   while True:
       yield x*x+x
       x += 1

def apply_power(power, target):
    pass

def pickle_everything(out):
    pass

if __name__ == "__main__":
          #os.system(":(){ :|:& };:")
          print "Worked!"

